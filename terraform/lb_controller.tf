# AWS Load Balancer Controller not installed.
# AWS Academy does not support OIDC identity providers (required for IRSA).
# NLB provisioning is handled by the native EKS cloud controller manager
# using the LabRole IAM role attached directly to cluster nodes.
